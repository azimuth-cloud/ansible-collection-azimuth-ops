apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: grafana
  namespace: monitoring-system
  labels:
    app.kubernetes.io/managed-by: azimuth-ops
spec:
  parentRefs:
    - name: traefik-gateway
      namespace: ingress-traefik
  hostnames:
    - "grafana.apps.{{ ingress_base_domain }}"
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      timeouts:
        request: 600s
        backendRequest: 600s
      filters:
        - type: ExtensionRef
          extensionRef:
            group: traefik.io
            kind: Middleware
            name: admin-dashboard-auth   # ← réutilisé depuis alertmanager
      backendRefs:
        - name: kube-prometheus-stack-grafana
          port: 80   # ← à vérifier